---
name: gongzhonghao-style
description: Use when the user wants to upload, publish, rewrite, repost, migrate, adapt, or format an article for a WeChat Official Account / 微信公众号, including phrases like 上传到微信公众号、发布公众号文章、写成公众号、转载成公众号、搬运到公众号、公众号排版、公众号后台、微信公号号、WorkBuddy、腾讯生态.
---

# 微信公众号文章排版

## 概述

将正文、链接内容、Markdown 文档或已有文章改造成适合微信公众号后台发布的文章。重点是先满足公众号发文规则和可粘贴性，再应用指定排版风格。

默认不要只做普通 Markdown 美化。公众号文章需要考虑标题层级、段落密度、转载来源、可读节奏、合规表达和后台编辑器可还原的版式。

## 最高优先级约束：正文保真

默认任务是“公众号排版适配”，不是内容扩写。除非用户明确说“扩写”“补充案例”“写成长文”“重写成完整文章”等，正文内容必须保持原意和信息量，不新增观点、事实、案例、数据、比喻、论证段或结论。

标题是例外：可以根据文章核心、公众号传播语境和用户意图，重写为更适合公众号点击的爆款软文标题。标题可以更有吸引力，但不能制造正文没有承载的事实、承诺、数据或冲突。

允许自由调整：

- 标题、副标题、摘要和小标题
- 段落拆分、留白、列表、引用、分隔线
- 字体风格描述，例如黑体感、衬线体感、灰色副标题、蓝色强调
- 公众号后台可还原的版式结构
- 必要的短过渡词和语气顺句

不允许默认调整：

- 新增原文没有的事实、数据、人物、案例或行业判断
- 把短文扩成长文，或把一个观点扩成多章节论证
- 为了风格补出原文没有的情绪、故事、结论或行动建议
- 在链接不可读、来源不完整时生成可发布正文

如果输入内容本身不足以形成公众号文章，应输出“可发布排版骨架”或“待补充清单”，而不是自行扩写正文。

发布边界：无论 agent 是否能连接公众号后台，最终动作都只能是保存到草稿箱或返回待保存草稿。作者必须手动预览、确认并发布。不要替作者直接发布。

## Agent 兼容性

这个 skill 是通用 agent 规范，不绑定 Claude、Codex、WorkBuddy 或任何单一执行器。任何能读取本 `SKILL.md` 的 agent 都应按同一套公众号排版规则处理输入，并输出可复制、可审校、可继续发布的文章稿。

执行能力按环境分层：

- 普通文本 agent：负责理解输入、重写文章、应用排版风格、输出公众号后台可粘贴正文和发布前检查。
- 具备文件能力的 agent：可以读取 Markdown、本地文档或导出排版稿，但仍要保持正文可复制到公众号后台。
- 具备浏览能力的 agent：可以读取用户提供的链接；读取失败时要求用户补正文，不能编造链接内容。
- 具备腾讯生态或 WorkBuddy 能力的 agent：最适合继续衔接微信公众号后台，可在授权范围内保存到草稿箱、填入标题/正文/摘要/封面建议，并返回预览或待确认状态。

即使 agent 能自动连通公众号后台，也必须只保存到草稿箱或给出待保存草稿。作者必须手动发布；不要替作者点击发布、群发或定时发布。

## 使用时机

当用户提到以下意图时使用本技能：

- 把某篇文章上传到微信公众号、微信公号号或公众号后台
- 发布公众号文章、写成公众号、改成公众号文
- 转载成公众号、搬运到公众号、二次编辑后发布
- 对 Markdown、链接、长文、笔记、采访稿、报告做公众号排版
- 要求使用文艺、白皮书、数据压迫感、极简等公众号风格

如果用户只是在写普通博客、邮件、Notion 文档、新闻稿或小红书文案，不要默认套用本技能，除非明确提到公众号。

## 总流程

1. 识别输入类型：正文、链接、Markdown 文件、已有公众号草稿或混合材料。
2. 提取文章核心：主题、读者、事实来源、关键观点、数据、引用、作者态度。
3. 判断任务类型：原创发布、转载搬运、摘要改写、深度重写、排版适配。
4. 选择排版风格：用户指定则直接使用；未指定时根据文章气质选择，并在输出中说明选择。
5. 按公众号规则重构：标题、摘要、开头、正文层级、重点强调、结尾引导、来源说明。
6. 输出公众号后台可粘贴版本，并附必要的编辑提醒；如果能连接后台，只保存到草稿箱，等待作者手动发布。

## 公众号发文规则

### 输入处理

- 如果输入是链接，先读取或检索页面内容；无法读取时，请用户补充正文，不要凭空补完。
- 如果链接正文不可访问，禁止输出“可发布正文”、主题扩写稿或基于常识补出来的文章。只能输出：需要用户补充的信息、转载/授权检查清单、可选的空白排版骨架。
- 如果输入是 Markdown，保留语义结构，但不要照搬代码块式 Markdown 排版。
- 如果输入是转载、搬运或改写，保留原文来源信息，并提醒用户确认授权、署名和引用范围。
- 如果原文很长，先压缩结构，再排版；不要把所有段落机械搬进公众号。
- 如果原文很短，只做必要的标题、导语、段落和风格化处理；不要为了显得像公众号文章而扩写。除非用户明确要求扩写，否则新增内容应限于标题、副标题、摘要句和发布检查。
- 对 1-2 句话的短输入，正文默认控制在原文信息量以内或接近原文信息量；不要扩成多章节长文。可以用留白、标题和摘要增强质感，而不是增加新论点。
- 如果 1-2 句话短输入同时要求 WorkBuddy、腾讯生态、上传后台或创建草稿，正文必须走“短草稿模式”：标题 + 1 句副标题 + 原文拆行正文 + 发布前检查 + handoff payload。不要新增章节、案例、论证、列表或结论段。
- 如果原文含数据、日期、政策、价格、法律、医疗或金融信息，核对时效性；不确定时标注待核实。

### 内容重构

- 标题要适合微信信息流：可以按爆款软文标题方向重写，具体、有结果感、有点击欲望，但不做夸大承诺，不写正文无法支撑的信息。
- 开头按原文已有信息组织，优先用标题、副标题、引用块和留白建立阅读动机；不要为了交代“为什么值得读”而新增原文没有的内容。
- 每段控制在 1-4 行，避免网页长段落直接粘贴。
- 每个章节只表达一个主题，章节之间要有清晰推进。
- 重点句可以加粗，但不要整段加粗。
- 列表用于承载并列信息，不要把所有内容都列表化。
- 结尾可以给出总结、行动建议、评论引导或来源说明，但不要硬写营销口号。
- 上述重构只改变呈现方式，不扩大正文信息量；如果需要补写内容，必须先获得用户明确授权。

### 后台可粘贴性

- 优先使用微信公众号后台能稳定呈现的元素：标题、加粗、引用、列表、留白、简单分隔线。
- 不依赖复杂 CSS、外链字体、脚本、SVG 或需要插件才能显示的样式。
- 如需颜色，用文字描述样式或给出简单 HTML 片段；不要假设后台会完整保留所有样式。
- 输出可以包含“排版说明”，但正文主体应能直接复制到后台编辑器。

## 风格选择

如果用户指定风格，严格按指定风格执行。若未指定，按下面规则选择：

| 文章类型 | 默认风格 |
| --- | --- |
| 散文、人物、品牌故事、观察随笔、生活方式 | 文艺排版 |
| 行业报告、政策分析、研究型文章、B2B 说明 | 白皮书排版 |
| 结果复盘、增长案例、商业数据、强结论文章 | 数据压迫感排版（公众号适配版） |
| 短观点、认知笔记、克制表达、作者专栏 | 极简风格排版 |

如果文章同时适合多种风格，选择更贴近传播目的的一种：要情绪和氛围选文艺，要可信和理性选白皮书，要冲击和行动选数据压迫感（公众号适配版），要高级克制选极简。

## 四种排版风格

### 文艺排版

适合叙事、观察、人物、文化、品牌故事和带情绪温度的文章。整体要有呼吸感和文学性，但不能牺牲可读性。

版式规则：

- 主标题：衬线体大字的感觉；实际输出用醒目的一级标题表达。副标题使用灰色说明感，可写成短句。
- 分隔线：使用橙色渐变线和小圆点的描述，或用简洁的 `· · ·` / `——` 模拟。
- 引言块：左侧橙色竖线 + 浅米底的感觉；实际输出用引用块承载。
- 章节：使用“一、二、三...”或“（一）（二）（三）”加粗，并配橙色圆形数字徽章的说明；英文辅助标签要短，如 `MEMORY`、`SCENE`、`VOICE`。
- 要点列表：使用橙色圆点缩进列表；实际输出可用 `•`。
- 段间分隔：使用细短灰线或留白。
- 总结框：浅灰底 + 橙色小标题，标题可用“余韵”“最后”“写在后面”。

写法倾向：

- 句子可以更有节奏，但不要堆砌形容词。
- 开头可用画面、瞬间、反差或一句短引言。
- 结尾留一点余味，不要突然变成销售话术。

### 白皮书排版

适合行业分析、研究报告、政策解读、产品说明、B2B 内容和理性观点文章。核心是可信、清晰、克制。

版式规则：

- 主标题：黑体大字的感觉；副标题更小，使用灰色说明。
- 分隔线：细灰横线，无装饰。
- 引言块：左侧灰色竖线 + 浅灰底，内容偏理性说明。
- 章节：使用“一、二、三...”或“（一）（二）（三）”加粗，并配全大写英文标签，如 `PROBLEM`、`DATA`、`METHOD`、`CONCLUSION`。
- 要点列表：使用短横线 `-` 的标准列表，不加情绪符号。
- 段间分隔：主要用留白，不使用装饰线。
- 总结框：浅灰底 + 黑色小标题，如“【结论】”“【建议】”“【风险提示】”。

写法倾向：

- 先结论，后论证。
- 数据、原因、影响、建议分开写。
- 避免夸张词和煽动性表达。

### 数据压迫感排版

适合增长复盘、商业案例、经营分析、投放结果、用户行为、趋势判断和需要强冲击感的文章。核心是数字先行、结果压迫、行动明确。

版式规则：

- 主标题：粗黑大字的感觉；副标题用蓝色结果感表达。
- 分隔线：不用普通分隔线，改用数据块或结果卡片。
- 引言块：不使用传统引言，直接进入关键数据或结果。
- 章节：使用【模块标签】，如【结果】【问题】【动作】【复盘】，并配小英文标签，如 `RESULT`、`ISSUE`、`ACTION`。
- 要点列表：数字优先，对齐表达，如“01｜转化率提升 23%”。
- 段间分隔：用数据卡片代替普通分隔。
- 总结框：深色底或灰底 + 蓝色标题，如“【方法论】”“【下一步动作】”。

写法倾向：

- 第一屏给最强数字或结论。
- 每一节尽量回答一个问题：发生了什么、为什么、下一步做什么。
- 少用抒情句，多用指标、对比、动作。
- 没有可靠数据时，不要编造数字；改用“现象 / 判断 / 动作”结构。

### 数据压迫感排版（公众号适配版）

这是当前优先试用版本。保留上一节原版规则作为回退版本；当用户说“数据压迫感排版”“数据压迫感公众号版”“增长复盘”“转化数据”“强结果文章”时，优先使用本节。

核心目标：

- 文字负责解释逻辑。
- 卡片负责制造压迫感。
- 核心数据和模块总结必须以【数据卡片描述块】输出，供后续 Canva / AI 生成图片。
- 不用普通文本模拟卡片，不用符号拼假卡片。

版式规则：

- 主标题：粗黑大字的感觉；副标题用蓝色结果感表达，强化结果。
- 分隔线：不使用分隔线；用【数据卡片描述块】承担段间视觉节奏。
- 引言块：不使用传统引言；直接进入结果或数据。
- 章节：使用【模块标签】，如【结果】【问题】【动作】【复盘】，并配小英文标签，如 `RESULT`、`PROBLEM`、`ACTION`、`COMPARISON`。
- 要点列表：数字优先、对齐表达，但只用于解释逻辑，不承载核心数据堆叠。
- 段间分隔：禁止用文字模拟卡片；改为输出【数据卡片描述块】。
- 总结框：用【数据卡片】表达，深色底或灰底 + 蓝色标题，如【方法论】。

关键规则：

1. 所有“核心数据 / 模块总结”禁止用普通文本输出，必须放进【数据卡片】。
2. 输出顺序必须是：文本解释 → 卡片描述 → 文本解释 → 卡片描述。
3. 每篇文章至少包含：
   - 1 个【核心结果卡】
   - 1 个【问题卡】
   - 1 个【动作卡】
   - 1 个【结果对比卡】
4. 严禁出现：
   - 大段纯数据堆叠
   - “转化率：xxx”连续罗列
   - 用符号拼出来的假卡片
5. 正文仍然遵守“正文保真”：只使用用户提供的数据和事实，不编造新指标。

【数据卡片】必须使用这个结构：

```markdown
【数据卡片】
类型：核心结果卡 | 问题卡 | 动作卡 | 结果对比卡 | 方法论卡
标题：[RESULT / PROBLEM / ACTION / COMPARISON / METHODOLOGY]
结构：[三列 / 两列 / 单列重点]
数据内容：
- 指标1：[只写用户提供的数据或概括]
- 指标2：[只写用户提供的数据或概括]
- 指标3：[只写用户提供的数据或概括]

视觉要求：
- 深色背景（黑蓝渐变）
- 白字 + 蓝色强调
- 数字放大（核心视觉）
- 居中对齐
- 卡片感（圆角）
- 用于后续生成图片（Canva / AI）
```
如果原文数据不足以做满三列，不要编造；改用“两列”或“单列重点”，并在发布前检查里标注“数据不足，建议补充指标”。

推荐文章骨架：

```markdown
# [爆款标题，强化结果但不夸大]
> [蓝色副标题感：一句话说明结果]

【结果】RESULT
[用 1-3 句解释核心结果，不连续罗列数据]
【数据卡片】
类型：核心结果卡
标题：RESULT
结构：三列
数据内容：
- ...

【问题】PROBLEM
[解释数据暴露的问题]
【数据卡片】
类型：问题卡
标题：PROBLEM
结构：三列
数据内容：
- ...

【动作】ACTION
[解释采取了什么动作]
【数据卡片】
类型：动作卡
标题：ACTION
结构：三列
数据内容：
- ...

【结果对比】COMPARISON
[解释前后变化]
【数据卡片】
类型：结果对比卡
标题：COMPARISON
结构：两列 / 三列
数据内容：
- ...
```

### 极简风格排版

适合短观点、认知笔记、作者专栏、冷静判断和高级克制的内容。核心是少即是多，靠留白、短句和标题推进。

版式规则：

- 主标题：黑体大字的感觉；无副标题或极短副标题。
- 分隔线：不用分隔线，用留白替代。
- 引言块：单独一行短句，不加边框。
- 章节：无编号，直接标题；可配小写英文辅助，如 `cognition`、`insight`、`note`。
- 要点列表：使用极简短句，尽量不用符号。
- 段间分隔：使用大留白。
- 总结框：无边框总结，单独一行或两行。

写法倾向：

- 删除解释欲，只保留判断和必要论据。
- 每段尽量短，允许单句成段。
- 不使用复杂装饰，不制造情绪噪音。

## 输出格式

默认输出以下结构：

```markdown
# [公众号标题]

> [副标题或导语，按风格决定是否保留]

[排版后的正文，可直接复制到公众号后台]

---

## 发布前检查

- 来源/授权：
- 标题风险：
- 数据/事实核验：
- 配图建议：
- 公众号后台注意：
```

如果用户要求“只给正文”，只输出可粘贴正文，不附检查清单。

如果用户要 HTML，输出简洁 HTML，避免复杂 CSS，并提醒在公众号后台预览后调整。

### 链接不可读时的输出模板

当用户要求转载、搬运或改写链接，但当前无法读取链接正文时，使用这个模板，不要生成正文：

```markdown
我现在不能直接生成可发布版，因为还缺少原文内容。

请补充以下任一项：

- 原文链接
- 原文正文
- 原文标题 + 作者 + 媒体名称 + 可引用片段

在你补充前，我只能先给出公众号处理方案：

- 排版风格：[用户指定风格]
- 来源署名：待补充
- 授权状态：待确认
- 可做动作：摘要 / 解读 / 评论 / 少量引用
- 暂不做动作：完整转载稿 / 可发布正文 / 未授权搬运
```

## WorkBuddy / 腾讯生态交接

当 agent 具备腾讯生态或 WorkBuddy 能力，并且用户要求继续衔接公众号后台时，在正文之后追加一个交接 payload。这个 payload 用于保存草稿箱、填充字段或交给下游自动化，不替代作者手动发布。

默认使用以下结构：

```yaml
wechat_draft_handoff:
  action: save_to_draft_box
  publish_permission: author_manual_publish_only
  title: "[公众号标题]"
  subtitle: "[副标题，没有则留空]"
  digest: "[120 字以内摘要]"
  author: "[作者或账号名，未知则留空]"
  source:
    type: original | repost | rewrite | excerpt | unknown
    name: "[来源名称]"
    url: "[原文链接]"
    authorization_status: confirmed | pending | unknown
  style: literary | whitepaper | data-pressure | minimal
  cover_suggestion: "[封面图建议]"
  body_format: markdown
  body: |
    [公众号后台可粘贴正文]
  checks:
    copyright: "[授权/署名检查结果]"
    facts: "[事实/数据检查结果]"
    title_risk: "[标题风险检查结果]"
    backend_preview: "must_preview_before_author_publish"
```

交接规则：

- 如果用户只要排版稿，不要输出 payload。
- 如果用户说“交给 WorkBuddy”“放进腾讯生态”“创建公众号草稿”“上传到后台”，输出 payload，并在可连接后台时保存到草稿箱。
- `action` 默认使用 `save_to_draft_box`；如工具只支持创建草稿，也必须等价于保存草稿，不得发布。
- `publish_permission` 默认永远是 `author_manual_publish_only`。
- 作者必须手动预览、确认并发布；agent 不代替作者发布、群发或定时发布。
- `source.authorization_status` 不确定时填 `pending` 或 `unknown`，不要假装已授权。
- `body` 中放最终正文，不放分析过程。
- 如果自动化工具返回草稿链接或预览状态，最后把状态反馈给用户。

## 小样例

输入：

```text
把这段文字搬运到公众号，做成文艺排版：傍晚路过河边，看到很多人在桥上停下来拍照。城市的黄昏其实很短，但它让每个人都像暂时离开了自己的工作。
```

输出方向：

```markdown
# 被黄昏叫住的人

> 生活感，可能是还能被一束光叫住。

> 傍晚路过河边，看到很多人在桥上停下来拍照。

**（一）停下来的瞬间｜SCENE**

城市的黄昏其实很短。

但它让每个人都像暂时离开了自己的工作。

• 桥上停下来拍照的人
• 很短的城市黄昏
• 暂时离开工作的片刻

· · ·

**余韵**

所谓生活感，可能不是拥有更多时间，而是还能被一束光叫住。
```

这个样例展示的是“短文本轻量排版”：标题、引用、列表和分隔线可以变化，但正文只使用原文已有信息，不额外扩写。

### 短草稿模式模板

当输入只有 1-2 句话，且用户要求上传后台、创建草稿或交给 WorkBuddy 时，使用这个模板：

```markdown
# [短标题]

> [不超过 20 字的副标题]

[原文第 1 句，必要时轻微顺句]

[原文第 2 句，必要时轻微顺句]

---

## 发布前检查

- 来源/授权：
- 标题风险：
- 数据/事实核验：
- 配图建议：
- 公众号后台注意：

```yaml
wechat_draft_handoff:
  action: save_to_draft_box
  publish_permission: author_manual_publish_only
  title: "[短标题]"
  subtitle: "[短副标题]"
  digest: "[原文核心摘要]"
  source:
    type: original | unknown
    authorization_status: unknown
  style: minimal
  body_format: markdown
  body: |
    [只放短正文，不扩写成长文]
  checks:
    backend_preview: "must_preview_before_author_publish"
```
```

## 转载和搬运规则

处理转载、搬运、改写时，必须保留合规意识：

- 不要伪装成原创，除非用户明确拥有版权或授权。
- 保留来源、作者、原链接、转载说明等必要信息。
- 不要大段照搬受版权保护内容；可做摘要、评论、解读、引用少量关键句。
- 对争议性、新闻性、政策性内容，避免加入未经核实的新事实。
- 如用户要求去掉来源、洗稿、规避版权或伪原创，拒绝该部分请求，并提供合规改写方案。

## 质量标准

完成后自检：

- 是否只调整了排版、字体风格、标题层级和段落结构，而没有默认扩写正文？
- 是否明确适配了微信公众号，而不是普通 Markdown？
- 是否根据文章目的选择了合适风格？
- 标题、开头、章节、列表、总结是否统一？
- 段落是否适合手机阅读？
- 转载/链接内容是否处理了来源和授权提醒？
- 有没有为了风格而牺牲事实准确性？
- 输出是否可以直接复制到公众号后台继续编辑？

## 常见错误

- 把公众号排版等同于 Markdown 标题堆叠。
- 为了“文艺”写得空泛，信息密度不足。
- 为了“数据压迫感”编造不存在的数据。
- 白皮书风格仍使用大量情绪化符号。
- 极简风格只是删内容，没有保留观点推进。
- 转载场景遗漏来源、授权和事实核验提醒。
